# Local configuration — contains the Cloudflare API token. NEVER commit it.
config.php

# Runtime state and logs generated by the script
vigilant-state.json
*.log

# Dependencies (none required, but just in case)
/vendor/
composer.lock

# OS / editor cruft
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
